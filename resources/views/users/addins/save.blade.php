<button
   class="btn btn-{{ $size }} btn-success"
   type="submit"
   {{-- formaction="{{ route($model.'s'.'.store') }}" --}}
   formmethod="POST"
   title="Save User">
   <i class="fa fa-save"></i>
</button>
