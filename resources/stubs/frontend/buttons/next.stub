@if($next)
   <a href="{{ route('{{modelNamePluralLowerCase}}.show', [$next]) }}"
      class="btn {{ $size ? 'btn-'.$size : '' }} btn-primary text-light"
      title="Next {{modelName}}">
      View Next
      <i class="{{ Config::get('buttons.next') }}"></i>
   </a>
@endif
