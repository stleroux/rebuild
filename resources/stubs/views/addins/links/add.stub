@if(checkPerm('{{modelNameSingularLowerCase}}_create'))
   <a href="{{ route('{{modelNamePluralLowerCase}}.create') }}"
      class="btn btn-sm btn-success"
      title="Add {{modelName}}">
      <i class="{{ Config::get('buttons.add') }}"></i>
   </a>
@endif
